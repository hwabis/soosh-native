syntax = "proto3";

package soosh;

enum ActionType {
  UNKNOWN = 0;
  JOIN = 1;
  START = 2;
  PLAY = 3;
}

message ClientMessage {
  ActionType action = 1;
  string payload = 2;
}

message ServerMessage {
  string status = 1;
  string data = 2;
}
